import type { SpellProgressionData } from "../types";
import rawProgressionData from "./spell-progression.json";

export const SPELL_PROGRESSION: SpellProgressionData = 
  rawProgressionData as SpellProgressionData;
